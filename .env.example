VITE_NEYNAR_DEMO=0

# Backend (serverless) config for real Neynar auth
# Provide your real values in deployment environment (Vercel project settings)
# or in local .env for testing.
#
# Full URLs to your Neynar auth endpoints (see Neynar docs for correct paths)
# Example placeholders below; replace with actual endpoints from Neynar.
# NEYNAR_AUTH_BEGIN_URL=https://api.neynar.com/v2/your/sign-in/begin
# NEYNAR_AUTH_POLL_URL=https://api.neynar.com/v2/your/sign-in/poll

# Neynar API key and header name (header defaults to 'api_key' if omitted)
# NEYNAR_API_KEY=YOUR_NEYNAR_API_KEY
# NEYNAR_API_KEY_HEADER=api_key

# Optional client identifier and redirect URI if your Neynar endpoint requires them
# NEYNAR_CLIENT_ID=YOUR_CLIENT_ID
# NEYNAR_REDIRECT_URI=https://your.app/callback

# Optional server-side poll tuning
# POLL_MAX_MS=60000
# POLL_INTERVAL_MS=1500

# Optional poll customization to match your provider
# If your poll endpoint expects token in Authorization header
# NEYNAR_POLL_AUTH_SCHEME=Bearer
# If it expects GET with token as query string
# NEYNAR_POLL_METHOD=GET
# NEYNAR_POLL_QUERY_KEY=token
# If it expects POST body but with a different key name
# NEYNAR_POLL_BODY_KEY=signer_uuid
# 실제 통합 시 DEMO를 0으로 바꾸고, 아래와 같은 서버 엔드포인트를 구현하세요.
# 백엔드는 Neynar API 키를 보관하며 /api/neynar/auth/begin, /api/neynar/auth/poll 등을 제공합니다.
